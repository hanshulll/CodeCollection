#include<bits/stdc++.h>
#define ll long long
using namespace std;
int main()
{
    ll n;
    while(cin>>n && n!=0)
    {
        ll a,i;
        vector<ll>st(2);
        vector<ll>arr(n);

        for(i=0;i<n;i++)
        {
            cin>>a;
            arr.push_back(a);
        }
        for(i=0;i<n;i++)
        {
            if(arr[i]<)
        }


    }
}
